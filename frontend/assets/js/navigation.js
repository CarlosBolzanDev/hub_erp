const links = [{ key:'dashboard', label:'Dashboard', icon:'▦', href:'/dashboard/' },{key:'orders',label:'Pedidos',icon:'□',href:'/orders/'},{key:'users',label:'Usuários',icon:'◉',href:'/users/'},{key:'picking',label:'Picking',icon:'⌁',href:'/picking/'},{key:'settings',label:'Configurações',icon:'⚙',href:'/settings/'}];
function renderNavigation(user) { const page = document.body.dataset.page; document.body.insertAdjacentHTML('afterbegin', `<div class="app-shell"><aside class="sidebar" id="sidebar"><a class="brand" href="/dashboard/"><span class="brand-mark">H</span>Hub ERP</a><nav class="nav">${links.map(link=>`<a class="nav-link ${page===link.key?'active':''}" href="${link.href}"><span>${link.icon}</span>${link.label}</a>`).join('')}</nav><div class="nav-spacer"></div><button class="theme-toggle" id="theme-toggle"><span>◐</span>Alternar tema</button><div class="sidebar-footer"><div class="user-summary"><span class="avatar">${(user?.name || 'U')[0].toUpperCase()}</span><div><strong>${user?.name || 'Usuário'}</strong><small>${user?.role || 'Conta'}</small></div></div><button class="nav-link" id="logout-button"><span>↪</span>Sair</button></div></aside><main class="main"><header class="topbar"><button class="menu-toggle" id="menu-toggle" aria-label="Abrir menu">☰</button><h1 class="page-title">${links.find(l=>l.key===page)?.label || 'Hub ERP'}</h1></header><section class="page-content" id="page-content"></section></main></div>`); document.querySelector('#theme-toggle').onclick=toggleTheme; document.querySelector('#logout-button').onclick=logout; document.querySelector('#menu-toggle').onclick=()=>{document.querySelector('#sidebar').classList.toggle('is-open');}; }
