import{apiRequest}from './api.js';import{applyTheme}from './theme.js';applyTheme();document.querySelector('#login-form').addEventListener('submit',async e=>{e.preventDefault();const error=document.querySelector('#login-error'),button=e.submitter;error.textContent='';button.disabled=true;button.textContent='Entrando…';try{const data=await apiRequest('/api/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});localStorage.setItem('accessToken',data.access_token);location.href='dashboard/'}catch(err){error.textContent=err.message}finally{button.disabled=false;button.textContent='Entrar'}});
