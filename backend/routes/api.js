const r=require('express').Router(); const prop=require('../controllers/propertyController'); const pay=require('../controllers/paymentController'); const dash=require('../controllers/dashboardController'); const rep=require('../controllers/reportController');
r.get('/dashboard',dash.index); r.get('/imoveis',prop.list); r.post('/imoveis',prop.create); r.get('/imoveis/:id',prop.get); r.put('/imoveis/:id',prop.update); r.delete('/imoveis/:id',prop.remove); r.post('/imoveis/:imovelId/pagamentos',pay.create); r.put('/pagamentos/:id',pay.update); r.delete('/pagamentos/:id',pay.remove); r.get('/relatorios/:type.csv',rep.csv); r.get('/relatorios/:type.pdf',rep.pdf); module.exports=r;
