// Header placeholder prepared for user menus, breadcrumbs, and global actions.
export function Header() {
  return (
    <header className="sticky top-0 z-10 border-b border-slate-200 bg-slate-50/90 px-8 py-4 backdrop-blur">
      <p className="text-sm font-medium text-slate-500">Painel administrativo</p>
    </header>
  );
}
