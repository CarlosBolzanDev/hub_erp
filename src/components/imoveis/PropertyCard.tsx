import Link from "next/link"; import type { Property } from "@/types/property"; import { formatCurrency } from "@/lib/utils/format";
export function PropertyCard({ property }: { property: Property }) { return <Link href={`/imoveis/${property.id}`} className="block rounded-2xl border bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-md animate-fade-up dark:border-slate-800 dark:bg-slate-900"><div className="flex justify-between gap-3"><h3 className="font-bold">{property.name}</h3><span className="rounded-full bg-blue-50 px-2 py-1 text-xs text-blue-700 dark:bg-blue-950 dark:text-blue-200">{property.status}</span></div><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">{property.owner_name ?? "Sem proprietário"}</p><div className="mt-4 flex justify-between text-sm"><span>{property.type ?? "—"}</span><strong>{formatCurrency(property.price)}</strong></div></Link>; }
