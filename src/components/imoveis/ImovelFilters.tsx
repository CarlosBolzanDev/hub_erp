import type { ImovelFilters as Filters } from '@/types/imoveis';
export const emptyFilters: Filters = { codigo:'', nome:'', proprietario:'', cidade:'', estado:'', tipo:'', situacao:'', faturasPendentes:false, faturasVencidas:false, aluguelMin:'', aluguelMax:'', vendaMin:'', vendaMax:'' };
export function ImovelFilters({ filters, onChange, onClear }: { filters: Filters; onChange: (f: Filters) => void; onClear: () => void }) {
  const set = (key: keyof Filters, value: string | boolean) => onChange({ ...filters, [key]: value });
  return <section className="filters">{(['codigo','nome','proprietario','cidade','estado','tipo','situacao'] as const).map(k=><label key={k}>{k}<input value={String(filters[k])} onChange={e=>set(k,e.target.value)} /></label>)}<label>Aluguel mín.<input type="number" value={filters.aluguelMin} onChange={e=>set('aluguelMin',e.target.value)} /></label><label>Aluguel máx.<input type="number" value={filters.aluguelMax} onChange={e=>set('aluguelMax',e.target.value)} /></label><label>Venda mín.<input type="number" value={filters.vendaMin} onChange={e=>set('vendaMin',e.target.value)} /></label><label>Venda máx.<input type="number" value={filters.vendaMax} onChange={e=>set('vendaMax',e.target.value)} /></label><label><input type="checkbox" checked={filters.faturasPendentes} onChange={e=>set('faturasPendentes',e.target.checked)} /> Faturas pendentes</label><label><input type="checkbox" checked={filters.faturasVencidas} onChange={e=>set('faturasVencidas',e.target.checked)} /> Faturas vencidas</label><button className="secondary" onClick={onClear}>Limpar filtros</button></section>;
}
