export type Proprietario = { id: string; nome?: string | null; email?: string | null; telefone?: string | null; documento?: string | null };
export type FaturaStatus = 'paga' | 'pendente' | 'vencida' | 'cancelada' | string;
export type Fatura = { id: string; proprietario_id: string; imovel_id: string; numero_parcela: number | null; total_parcelas: number | null; valor_parcela: number | null; data_vencimento: string | null; data_pagamento: string | null; juros: number | null; multa: number | null; desconto: number | null; valor_pago: number | null; status: FaturaStatus | null; observacoes: string | null };
export type Imovel = { id: string; proprietario_id: string; codigo: string; nome: string; tipo: string | null; cep: string | null; endereco: string | null; numero: string | null; complemento: string | null; bairro: string | null; cidade: string | null; estado: string | null; area_total: number | null; area_construida: number | null; matricula: string | null; cartorio: string | null; valor_venda: number | null; valor_aluguel: number | null; situacao: string | null; observacoes: string | null; created_at: string; updated_at: string | null; proprietarios?: Proprietario | null; faturas?: Fatura[] };
export type ImovelFormData = Omit<Imovel, 'id' | 'created_at' | 'updated_at' | 'proprietarios' | 'faturas'>;
export type ImovelFilters = { codigo: string; nome: string; proprietario: string; cidade: string; estado: string; tipo: string; situacao: string; faturasPendentes: boolean; faturasVencidas: boolean; aluguelMin: string; aluguelMax: string; vendaMin: string; vendaMax: string };
