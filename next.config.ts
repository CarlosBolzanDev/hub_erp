import type { NextConfig } from "next";

// Central Next.js configuration for App Router, builds, and future framework options.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
