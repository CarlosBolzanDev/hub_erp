# Embedded Python runtime

Place the Python embeddable distribution here for production use:

- `python.exe`
- `python312.dll`
- standard library files or zip
- vendored packages under `site-packages/`

VoltPy resolves this directory relative to the copied engine folder: `<App>/VoltPy/runtime`.
